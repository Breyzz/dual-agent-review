# Copy this file to .env and fill in your real keys.
# .env is gitignored — never commit real keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional overrides — leave blank to use the defaults in providers.py
CLAUDE_MODEL=
GPT_MODEL=
